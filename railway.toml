[build]
builder = "NIXPACKS"
buildCommand = "pip install -r chat-api/requirements.txt"

[deploy]
startCommand = "python3 chat-api/chat_api.py"
healthcheckPath = "/"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
